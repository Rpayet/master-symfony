{% extends "base.html.twig" %}

{% block title %}
Produit seul
{% endblock %}

{% block body %}

<h1>{{ post.name }}</h1>
<p>{{ post.content }}</p>
<p>{{ post.publishedAt.format('d/m/Y') }}</p>

{% endblock %}